<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>CleverManagement</title>
</head>
<body>
	<h3>{{task.name|raw}}</h3>

	<p>
		{{task.description|raw}}
	</p>
	<br><br>
	<a href="/Clevermanagement/{{ project_id }}/step/{{ step_id }}/task/{{ task.id }}/edit">Editer</a> | <a href="/Clevermanagement/{{project_id}}/step/{{step_id}}/task/{{ task.id }}/delete">Supprimer</a><br>
</body>
</html>
