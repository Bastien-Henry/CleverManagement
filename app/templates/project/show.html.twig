<!doctype html>
<html lang="en">
	<head>
	    <meta charset="UTF-8">
  		<title>Clever Management - Make your projects crystal</title>
  		<!-- Latest compiled and minified CSS -->
		<link rel="stylesheet" href="/CleverManagement/assets/styles/bootstrap.css">
		<link rel="stylesheet" href="/CleverManagement/assets/styles/main.css">
		<link rel="stylesheet" href="/CleverManagement/assets/styles/select2.css">
	</head>
 	<body>
 		<div class="navbar">
		  <div class="navbar-inner">
		    <a class="brand" href="#"><img src="/CleverManagement/assets/images/logo.png"></a>
		    <ul class="nav">
		      <li><a href="/CleverManagement">Accueil</a></li>
		      <li><a href="/CleverManagement/deconnection">Deconnexion</a></li>
		    </ul>
		  </div>
		</div>
		<div class="container-fluid">
		  <div class="row-fluid">
		    <div class="span2 sidebar">
		    	<ul class="span2 sidebar-menu">
		    		<a href="new-project.php">
		    			<li class="current">
		    				<span class="text-li-sidebar">
		    					<a href="/CleverManagement/create" class="text-li-sidebar">Nouveau projet</a>
		    				</span>
		    			</li>
		    		</a>
		    		<a href="/CleverManagement/directory/create" class="text-li-sidebar">
		    			<li>
		    				<span class="text-li-sidebar">Nouveau dossier
		    				</span>
		    			</li>
		    		</a>
		    		{% for directory in directories %}
		    		<a href="/CleverManagement/directory/{{directory.id}}/show" class="text-li-sidebar">
		    			<li>
		    				<span class="text-li-sidebar">
		    					{{directory.name}}
		    				</span>
		    			</li>
		    		</a>
		    		{% endfor %}
		    	</ul>
		      <!--Sidebar content-->
		    </div>
		<div>
			<div class="name-project"><span style="position:relative;top:5px;">Projet : {{project.name|raw}}</span></div>

			<div class="description-project">
				<p>
					<h3>Description du projet : </h3>
					{{project.description|raw}}
				</p>
				<p>
					Debut : {{project.startline}} <br>
					Fin : {{project.deadline}}
				</p>
				<p>
					Nombre d'heures passé sur le projet {{ hour_project }} h<br>
					Prix consacré au projet {{ price_project }} €<br>
				</p>
				<p>
					Admins : 
					{% for admin in admins %}
						{% if loop.first %}
							{{ admin.email }}
						{% else %}
							<span style="font-weight:bold;font-size:20px">|</span> {{ admin.email }}
						{% endif %}
					{% endfor %}
				</p>
				<p>
					Membres : 
					{% for member in members %}
						{% if loop.first %}
							{{ member.email }}
						{% else %}
							<span style="font-weight:bold;font-size:20px">|</span> {{ member.email }}
						{% endif %}
					{% endfor %}
				</p>
				{% if status == 1 %}
					<div class="alert alert-error" style="width:200px;text-align:center;">
						Statut : To Do
					</div>
				{% elseif status == 2 %}
					<div class="alert alert-warning" style="width:200px;text-align:center;">
						Statut : Doing
					</div>
				{% else %}
					<div class="alert alert-success" style="width:200px;text-align:center;">
						Statut : Done
					</div>
				{% endif %}
			</div>
			<div class="action-project">
				<a href="/CleverManagement/{{project.id}}/edit" title="Editer le projet">
	      			<span>
	      				<img src="/CleverManagement/assets/images/edit.png">
	      			</span>
	      		</a>
	      		<a href="/CleverManagement/{{project.id}}/delete" title="Supprimer le projet">
		  			<span>
		  				<img src="/CleverManagement/assets/images/delete.png">
		  			</span>
		  		</a>
	  		</div>
	  		<div class="steps-project">
	  			<div class="steps">
		  			<button class="btn btn-primary"><a href="/Clevermanagement/{{project.id}}/step/create" style="color:#fff;">Nouvelle etape</a></button>
	  			</div>

				<br><br>
				<strong>{{message}}</strong><br>
				<br><br>
				{% for step in steps %}
				<a href="/Clevermanagement/{{project.id}}/step/{{step.id}}/show">
					<div class="one-step">
						<div class="title-steps-project">
							<span style="position:relative;top:5px;">{{ step.name }} du {{step.startline}} au {{step.deadline}} status = {{ step.status }}</span>
							<a href="/Clevermanagement/{{project.id}}/step/{{step.id}}/edit" title="Editer l'&eacute;tape">
				      			<span>
				      				<img src="/CleverManagement/assets/images/edit.png">
				      			</span>
				      		</a>
				      		<a href="/Clevermanagement/{{project.id}}/step/{{step.id}}/delete" title="Supprimer le projet">
					  			<span>
					  				<img src="/CleverManagement/assets/images/delete.png">
					  			</span>
					  		</a>
						</div>
						<div class="description-steps-project">
							<span style="position:relative;top:10px;">{{ step.description }}</span>
						</div>
					</div>
				</a>
				{% endfor %}
	  		</div>
		</div>
	</body>
</html>


